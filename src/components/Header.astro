<header>
  <a class="header-logo" href="/">LOGO</a>

  <!-- Десктопная навигация (скрыта на мобильных) -->
  <nav class="desktop-nav">
    <a href="/" class="header__link">Главная</a>
    <a href="/news" class="header__link">Новости</a>
    <a href="/archive" class="header__link">Статьи</a>
    <a href="/galleries" class="header__link">Галерея</a>
    <a href="/about" class="header__link">О себе</a>
  </nav>

  <!-- Кнопка бургера (видна только на мобильных) -->
  <button class="burger" popovertarget="navMenu" popovertargetaction="toggle" aria-label="Меню"> </button>

  <!-- Модальное меню (мобильное) -->
  <nav id="navMenu" class="mobile-menu" popover>
    <button popovertarget="navMenu" popovertargetaction="hide" class="close-btn" aria-label="Закрыть меню"
      >✕</button
    >
    <div class="mobile-links">
      <a href="/" class="mobile-link">Главная</a>
      <a href="/news" class="mobile-link">Новости</a>
      <a href="/archive" class="mobile-link">Статьи</a>
      <a href="/about" class="mobile-link">О себе</a>
    </div>
  </nav>
</header>

<style>
  /* === ОСНОВНЫЕ СТИЛИ ХЕДЕРА === */
  header {
    display: grid;
    grid-template-columns: auto 1fr auto;
    grid-template-rows: 4rem;
    align-items: center;
    justify-items: center;
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 2rem;
    background: white;
    z-index: 100;
    background-color: var(--accent-primary);
    border-radius: 2rem;
    border: var(--border);
    border-color: var(--accent-glow);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    width: 80%;
    margin: 0 auto;
  }

  /* === ДЕСКТОПНОЕ МЕНЮ === */
  .desktop-nav {
    display: flex;
    gap: 1.5rem;
  }

  .header__link {
    text-decoration: none;
    color: var(--text);
  }

  .header__link:hover {
    color: var(--accent);
  }

  /* === БУРГЕР (СКРЫТ ПО УМОЛЧАНИЮ) === */
  .burger {
    display: none;
    width: 24px;
    height: 24px;
    background-color: var(--text, #333);
    -webkit-mask-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="24" height="24"><path d="M23 20L23 6L6 6L6 20L23 20Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/><path d="M42 42V28L25 28L25 42H42Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/><path d="M31 6V20H42V6H31Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/><path d="M6 28L6 42H17V28H6Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/></svg>');
    mask-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 48 48" width="24" height="24"><path d="M23 20L23 6L6 6L6 20L23 20Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/><path d="M42 42V28L25 28L25 42H42Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/><path d="M31 6V20H42V6H31Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/><path d="M6 28L6 42H17V28H6Z" fill="none" stroke="black" stroke-width="4" stroke-linejoin="round"/></svg>');
    -webkit-mask-size: contain;
    mask-size: contain;
    -webkit-mask-repeat: no-repeat;
    mask-repeat: no-repeat;
    -webkit-mask-position: center;
    mask-position: center;
  }

  /* === МОБИЛЬНОЕ МЕНЮ (POPOVER API) === */
  #navMenu[popover] {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    margin: 0;
    width: 90vw;
    max-width: 400px;
    padding: 2rem;
    border-radius: 1rem;
    background: var(--accent-primary, #ffffff);
    border: 1px solid var(--border);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
    z-index: 1000; /* Поверх хедера */
  }

  /* Затемнение фона (Backdrop) */
  #navMenu::backdrop {
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(4px);
  }

  /* Ссылки внутри модалки */
  .mobile-links {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    align-items: center;
    margin-top: 1rem;
  }

  .mobile-link {
    font-size: 1.5rem;
    text-decoration: none;
    color: var(--text);
    padding: 0.5rem;
    width: 100%;
    text-align: center;
  }

  .mobile-link:hover {
    color: var(--accent);
  }

  /* Кнопка закрытия внутри модалки */
  .close-btn {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: none;
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    color: var(--text);
    padding: 0.5rem;
  }
</style>
